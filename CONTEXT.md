# CRUX 领域上下文

CRUX 是一个把攀岩墙照片转成可修正路线记录的本地应用。本文是产品语义的唯一词汇表；产品讨论、数据模型、界面文案和测试描述优先使用这里的术语。

## 场景与对象

**岩馆（Gym）**：用户实际攀爬的实体场所。一个岩馆可以有多条路线；路线记录保存发生时的岩馆信息，不把岩馆误当成路线。
_Avoid_: 场馆、健身房、地点

**路线（Route）**：墙面上由一组同色或同路线标识岩点构成的攀爬目标。路线是墙面对象，不等于用户的一次尝试或一条记录。
_Avoid_: 线路、攀爬顺序

**路线记录（Route Record）**：用户在某个时间、某个岩馆对一条路线的记录，包含等级、结果、尝试次数、用时、备注和路线图像。它是产品中可保存、可回看的主要对象。
_Avoid_: 训练记录、路线实例、攀爬序列

**岩点（Climbing Feature）**：墙面上可被识别并参与路线选择的攀爬特征的统称，包括小握点和体积块。
_Avoid_: 握点（当作所有特征的唯一称呼）、目标点

**小握点（Model Hold）**：模型分类中的小型手点或脚点，是岩点的一种；“Hold”不是所有岩点的业务总称。
_Avoid_: 用小握点指代体积块、用 Hold 指代全部岩点

**体积块（Volume）**：墙面上较大的立体攀爬结构，是岩点的一种，不因为面积大就自动成为路线的一部分。
_Avoid_: 大握点、障碍物

## 识别与选路

**标准照片（Canonical Photo）**：经过方向修正、色彩空间统一和尺寸规范化后，供识别、取色、渲染共同使用的照片。所有后续坐标和颜色判断都以同一张标准照片为准。
_Avoid_: 原图（当作算法坐标基准）、预览图

**参考色（Reference Color）**：从单个岩点的 mask 覆盖区域提取出的墙面颜色特征，用于同色候选匹配。参考色不是用户填写的路线颜色标签，也不是模型预测类别。
_Avoid_: 路线颜色、主色

**路线颜色（Route Color）**：用户看到并选择的路线颜色标签，例如蓝、红、黄。它是路线记录的展示属性，不代表模型直接预测了颜色。
_Avoid_: 参考色、模型颜色

**种子点（Seed）**：用户在照片中首先点选的一个岩点，用来表达“我想找这条路线”。种子点不是自动推断的起点，也不等于 START。
_Avoid_: 起点、首个动作点

**候选岩点（Candidate）**：与种子点颜色相近、尚未经过空间分组的岩点集合成员。候选岩点不等于最终路线成员。
_Avoid_: 路线点、已选岩点

**路线组（Route Group）**：候选岩点经过空间关系分组后形成的集合；默认选择包含种子点的路线组。路线组只表达视觉上的同一路线候选，不表达攀爬顺序或 beta。
_Avoid_: 攀爬序列、动作链、路线顺序

**分析结果（Route Analysis）**：标准照片经过岩点识别、参考色提取和种子选路后得到的可检查结果。分析结果尚未成为用户确认的路线记录。
_Avoid_: 识别结果（过于宽泛）、最终路线

**路线草稿（Route Draft）**：用户可以继续加入、移除、标记起终点的分析结果。草稿只有在用户确认后才形成路线记录。
_Avoid_: 临时记录、半成品路线

## 人工修正与用户语义

**漏选（Missed Selection）**：模型已经识别出岩点，但默认路线组没有选中它。用户点击该岩点即可加入，不应称为模型漏检。
_Avoid_: 漏检、识别失败

**漏检（Missed Detection）**：照片中存在岩点，但模型没有产生对应识别结果。用户需要使用补点操作表达它；漏检与漏选必须分开统计。
_Avoid_: 漏选、误识别

**修正（Correction）**：用户对路线草稿做出的加入、移除或起终点标记操作。修正是用户对分析结果的明确覆盖，不是模型再次推理。
_Avoid_: 编辑（编辑可泛指任何字段修改）、训练样本

**补点（Manual Addition）**：针对漏检岩点，用户手动创建一个岩点近似区域并加入草稿。MVP 中补点是简单圆形或椭圆区域，不是自由画笔。
_Avoid_: 添加候选、自动补全

**起点（START）**：用户明确标记为路线开始位置的岩点，可以有多个。系统不根据最低位置自动生成起点。
_Avoid_: 最低点、首点

**终点（FINISH）**：用户明确标记为路线结束位置的岩点，可以有多个。系统不根据最高位置自动生成终点。
_Avoid_: 最高点、最后点

**路线脊柱（Route Spine）**：从下向上连接路线岩点的视觉引导线，用于帮助阅读路线。它不代表真实攀爬顺序、动作建议或 beta。
_Avoid_: 攀爬路线、动作顺序、beta 线

## 结果与模型数据

**路线结果（Route Result）**：用户对本次攀爬的主观结果：FLASH、TOP 或 PROJECT。它由用户选择，不由尝试次数、起终点或模型自动推导。
_Avoid_: 自动状态、完成度评分

**研究模型（Research Model）**：用于验证识别链路、算法和部署可行性的模型，不代表可以随 App 发布。
_Avoid_: 生产模型、正式模型

**生产模型（Production Model）**：满足数据授权、设备性能和独立评估要求后，允许随 App 分发的模型。研究模型通过实验不自动升级为生产模型。
_Avoid_: 当前默认模型、最好模型

**纠正集（Correction Set）**：用于记录人工纠错信号并比较迭代效果的数据集合。若其中的图片参与过训练，它不能被称为独立测试集。
_Avoid_: 测试集（当存在训练泄漏时）、泛化验证集

**独立测试集（Independent Test Set）**：训练和纠正过程均未使用、并按明确数据边界保留到最终评估的数据集合。只有它可以支持独立泛化结论。
_Avoid_: realpic（未说明是否参与训练时）、开发集
