# CRUX Agent Rules

这份文件规定代理在本仓库里的工作方式。规则短，但必须执行。

## 1. 唯一真相源

同一件事只保留一个权威来源。其他文档只能引用它，不能复制一份容易过期的版本。

- 产品范围、架构和验收标准：看 `PLAN.md`。
- 领域名词和产品语义：看 `CONTEXT.md`。
- 代码行为：看代码和测试。
- 配置行为：看配置文件和命令帮助。
- 实际运行结果：以刚运行得到的结果为准。

发现两处说法不一致时：

1. 先判断哪一处是真相源；
2. 修正真相源；
3. 只更新受影响的引用或说明；
4. 不新增第三份解释。

涉及产品语义、功能分区或数据命名时，先读 `CONTEXT.md`。本仓库只使用 `AGENTS.md`，不创建或保留 `CLAUDE.md`。

## 2. 完整链路验证

不要只证明某个函数能运行。要从用户输入一路追到用户看到的结果。

按这个顺序检查：

```text
用户入口
→ UI 状态
→ coordinator
→ domain module
→ adapter / 外部库
→ 持久化
→ 查询 projection
→ 屏幕输出
```

修改任何一层时，都要检查：

- 上游传进来的数据是否仍符合约定；
- 下游是否仍能读取、保存和展示；
- 错误、空数据、取消和重复操作是否有明确结果；
- 真正的入口是否能走到真正的输出。

只通过孤立单元测试，不算完整验证。没有实际运行验证，不声明“已完成”。

## 3. 第一性原则

先回答四个问题，再改代码：

1. 真正要解决的问题是什么？
2. 输入和输出是什么？
3. 哪些条件必须始终成立？
4. 最小可行改动是什么？

遇到不清楚的 API、库行为或工具用法：

1. 查官方文档和官方示例；
2. 再读本地安装包或官方源码；
3. 仍不足时，上网查可靠的一手来源；
4. 记录已确认的事实，不用猜测替代证据。

优先复用现有模块。没有实际需求，不新增抽象、依赖、兼容层或配置项。

## 项目硬约束

- deployment target = iOS 17.0；iOS 18+ API 必须有 `#available` 降级路径。
- 全链使用 Apache-2.0、MIT、BSD 等 permissive 许可；禁止 Ultralytics/AGPL。
- Kaggle 数据只用于 research；production weights 只使用自有或明确授权数据。
- 新增脚本提供 `--selfcheck`，并实际运行自检。
- Windows 可推进数据、模型和 `CRUXCore`；iOS UI 编译交给 macOS CI。

## 完成前检查

- 真相源没有被绕过；
- 相关上下游已经检查；
- 关键路径已经实际运行；
- 测试或 smoke check 有结果；
- 未提交临时文件、密钥、cookie、截图或模型大文件。
