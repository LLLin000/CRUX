# D0.5 — RF-DETR-Seg 链路 spike（ONNX 路线）

- **Blocking edges**: D0
- **Runs on**: Windows（本机，RTX 5060 8GB）+ iPhone 真机（Gate C）
- **Scope**: 只 gate runtime/export，不 gate hold recall（recall 归 D1/D2）；10-20 张小数据微调（research weights）→ ONNX 导出 → iOS onnxruntime 推理

## Acceptance
- [x] **Gate A**：PyTorch → ONNX 完整导出（Windows 端 torch 对比），推理数值一致
- [x] **Gate B**：ONNX 输出正确（mask shape/bbox 正确、无 crash、数值无异常）
- [ ] **Gate C**：真机 384/432/512 三档 p50/p95/peak memory（待 iPhone）

## 状态
- **Gate A/B 完成**（2026-08）：小数据微调 + ONNX 导出 + 推理验证通过；训练配置 `tools/rfdetr_hold.yaml`，spike 产物见 `.scratch/`
- **Gate C 待办**：等 iPhone 真机跑 onnxruntime 延迟基准；三档分辨率结果决定冻结输入分辨率与模型档位（Nano/S/M）
- 若 A/B/C 出现 runtime 兼容问题 → 切 RTMDet-Ins（备选）

## Blocking
- blocks: D2（模型冻结）
